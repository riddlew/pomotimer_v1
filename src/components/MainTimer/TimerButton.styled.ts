import styled from "styled-components";

export const TimerButton = styled.button`
	border: 0;
	background: transparent;
	font-size: min(4rem, min(15vw, 15vh));
	color: white;
	cursor: pointer;
	width: 25%;
`;
